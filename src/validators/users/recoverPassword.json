{
  "type": "object",
  "required": [
    "token"
  ],
  "properties": {
    "token": {
      "type": "string",
      "minLength": 30,
      "maxLength": 60,
      "pattern": "^(?=.*?[a-zA-Z])(?=.*?[0-9])(?=.*?[-]).{30,}$"
    }
  }
}
