{
  "type": "object",
  "required": [
    "old_password",
    "new_password"
  ],
  "properties": {
    "old_password": {
      "type": "string",
      "minLength": 7,
      "maxLength": 50,
      "pattern": "^[a-zA-Z0-9]*$"
    },
    "new_password": {
      "type": "string",
      "minLength": 7,
      "maxLength": 50,
      "pattern": "^[a-zA-Z0-9]*$"
    }
  }
}